<h1>Welcome to the Band Show Page</h1>
<h3>Name: <%= @band.name %></h3>
<h3>Location: <%= @band.city %>, <%= @band.state %></h3>
<h3>Artists</h3>
<ol>
  <% @band.artists.each do |artist| %>
    <li><%= link_to artist.name, artist_path(artist) %></li>
  <% end %>
</ol>

<p><%= link_to "Back to All Bands", bands_path %></p>